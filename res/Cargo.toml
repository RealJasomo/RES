[package]
name = "res"
description = "A rust based NES emulator"
version = "0.1.0"
authors = ["RealJasomo"]
edition = "2020"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
